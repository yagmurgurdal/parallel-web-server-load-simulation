{
  "name": "parallel-web-server-load-simulation-server",
  "version": "1.0.0",
  "description": "Node.js server for comparing blocking and non-blocking request handling.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
